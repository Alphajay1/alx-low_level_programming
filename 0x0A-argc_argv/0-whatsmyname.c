#include <stdio.h>

/**
 * main - prints program name, followed by a new line.
 * @argc: argument size of array.
 * @argv: array size of arguments.
 *
 * Return: 0 on success.
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
