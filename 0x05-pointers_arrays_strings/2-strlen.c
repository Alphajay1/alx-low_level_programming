#include "main.h"

/**
 * _strlen - returns the length of a string.
 * @s: string to be counted.
 *
 * Return: number of characters.
 */
int _strlen(char *s)
{
	int lenght;

	lenght = 0;
	while (lenght[s] != '\0')
	{
		lenght++;
	}
	return (lenght);
}
