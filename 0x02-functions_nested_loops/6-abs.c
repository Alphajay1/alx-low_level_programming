#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @x: value to be checked.
 * 
 * Return: absolute value of int x.
 */
int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	else
	{
		return (-x);
	}
}
